<% @page_title = "Import Records" %>

<h1>Import Records</h1>


<p>Export a spreadsheet (Excel or Google Sheets) to CSV format.</p>
<ul>
  <li>
    For <strong>Google Sheets</strong> spreadsheet: <br>
    File -> Download as -> Comma-separated values (.csv, current sheet)
  </li>

  <li>
    For <strong>Excel</strong> spreadsheet: <br>
    [TODO...]
  </li>
</ul>
<p>Select the CSV file to import:</p>

<br>

<div>
  <%= form_tag import_spreadsheets_path, multipart: true do %>

    <!-- ADD EVENT SELECTOR HERE -->


    <%= file_field_tag :uploaded_file, {:class => 'file-upload'} %>

    <!--
    <div class="input-group">
      <div class="input-group-prepend">
        <span class="input-group-text" id="inputGroupFileAddon01">Upload</span>
      </div>
      <div class="custom-file">
        <%#= file_field_tag :uploaded_file, {:class => 'file-upload', :id => 'inputGroupFile01'} %>
        <label class="custom-file-label" for="inputGroupFile01">Choose file</label>
      </div>
    </div>
    -->

    <br>
    <br>

    <%= submit_tag "Import CSV", {:class => 'btn btn-success btn-block'} %>

  <% end %>

</div>
