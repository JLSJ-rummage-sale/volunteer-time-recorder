<% @page_title = "Add Event" %>

<h1>Add Event</h1>

<%= form_for :event, url: events_path do |form| %>

    <%= render(:partial => 'form', :locals => {:form => form}) %>

    <p>
        <%= form.submit("Add Event", {:class => 'btn btn-success btn-block'}) %>
    </p>
<% end %>
